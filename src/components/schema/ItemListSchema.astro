---
interface Article {
    title: string;
    preview_text: string;
    original_img_url?: string | null;
    id_str: string;
    screen_name: string;
    slug?: string;
    created_at?: string;
}

interface Props {
    items: Article[];
    listName: string;
}

const { items, listName } = Astro.props;

const itemListElements = items.slice(0, 100).map((tool, index) => ({
    "@type": "ListItem",
    position: index + 1,
    item: {
        "@type": "Article",
        "@id": `https://xarticl.es/articles/${tool.slug}`,
        name: tool.title,
        url: `https://xarticl.es/articles/${tool.slug}`,
        description: tool.preview_text,
        ...(tool.screen_name ? { author: { "@type": "Person", name: `@${tool.screen_name}`, url: `https://x.com/${tool.screen_name}` } } : {}),
        ...(tool.created_at ? { datePublished: tool.created_at } : {}),
    },
}));

const schema = {
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: listName,
    numberOfItems: items.length,
    itemListElement: itemListElements,
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
